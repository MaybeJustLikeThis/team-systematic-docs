# 任务闭环系统 (Task Loop) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Claude Code 里实现一个单任务闭环系统——AI 改代码前先锁定范围，PLAN/BUILD/CLOSE 三拍由人发车、guardian 守卫硬拦截，计划与收尾各提交一次知识到 memory/draft。

**Architecture:** 单点状态文件 `.ai/task.json` 驱动；一个 PreToolUse 守卫脚本 `guardian.sh` 读状态拦截越界写入；四个 bash 脚本（lock/build/close/extend）驱动状态机 + gate 验证；四个 slash command 触发对应脚本。知识沿用 MEM-* 体系，AI 只写 `memory/draft/`。

**Tech Stack:** Bash（Git Bash on Windows）、jq（JSON 处理）、Claude Code hooks + slash commands。零运行时框架依赖，测试用纯 bash 断言脚本。

**关联 spec:** `docs/superpowers/specs/2026-06-17-task-loop-design.md`

---

## 技术背景（实现者必读）

### Claude Code PreToolUse hook 机制
- 在 `.claude/settings.json` 的 `hooks.PreToolUse` 注册，`matcher` 匹配工具名。
- hook 脚本通过 **stdin** 收到一段 JSON：
  ```json
  { "tool_name": "Write", "tool_input": { "file_path": "/abs/path", "content": "..." } }
  ```
  对 Bash 工具，`tool_input.command` 是命令字符串。
- **退出码语义**：`0` = 放行；`2` = 阻断，stderr 内容反馈给 AI；其他 = 非阻断错误。
- guardian 用退出码 2 实现硬拦截。

### task.json schema
```json
{
  "task_id": "TASK-YYYYMMDD-slug",
  "stage": "PLAN",
  "human_owner": "",
  "scope": {
    "allowed_paths": ["src/pay/refund/"],
    "blocked_paths": ["src/auth/", "infra/prod/"],
    "extra_grants": []
  },
  "gate": {
    "plan_confirmed": false,
    "pre_committed": false,
    "tests_passed": false,
    "review_done": false,
    "post_committed": false
  }
}
```
- `stage` ∈ `PLAN | BUILD | CLOSE | DONE`
- **范围用前缀匹配**（不是 glob）：`allowed_paths` 存路径前缀（目录带尾斜杠或具体文件），guardian 判断目标路径是否以某前缀开头。spec 里的 `**` 在实现里等价于"该前缀下所有文件"。

### 路径规范化
Claude Code 给的 `file_path` 是绝对路径。guardian 需先转成相对项目根的路径再匹配：`REL="${TARGET#"$PWD"/}"`。

### 测试方式（零依赖）
不用 bats，用纯 bash 断言。`tests/test-helpers.sh` 提供 `assert_eq` / `assert_match`，每个测试文件 source 它后跑断言，结尾打印 PASS/FAIL 计数。所有测试可被 `tests/run-all.sh` 串起来。

### jq 用法速查
- 读字段：`jq -r '.stage' task.json`
- 读数组：`jq -r '.scope.allowed_paths[]?' task.json`
- 写字段：`jq '.gate.plan_confirmed = true' task.json > tmp && mv tmp task.json`
- 追加数组元素：`jq '.scope.extra_grants += ["x"]' task.json`

---

## 文件结构总览

```text
.claude/
  settings.json              注册 guardian 为 PreToolUse hook
  commands/
    lock.md                  /lock  [paths]  → 跑 task-lock.sh
    build.md                 /build          → 跑 task-build.sh
    close.md                 /close          → 跑 task-close.sh
    extend.md                /extend [path]  → 跑 task-extend.sh
  scripts/
    lib-state.sh             task.json 读写辅助函数（被各脚本 source）
    task-lock.sh             初始化 task.json + 进 PLAN
    task-build.sh            gate 检查 + 进 BUILD
    task-close.sh            gate 检查 + 进 CLOSE / DONE
    task-extend.sh           扩 extra_grants
  hooks/
    guardian.sh              PreToolUse 守卫
.ai/
  task.json                  （运行时生成，不在仓库里提交）
  memory/draft/              AI 知识候选（运行时）
  memory/active/             已激活知识（运行时）
tests/
  test-helpers.sh            断言辅助
  run-all.sh                 串联所有测试
  test-lib-state.sh
  test-guardian.sh
  test-lock.sh
  test-build.sh
  test-close.sh
  test-extend.sh
  fixtures/                  测试用 task.json 样本
CLAUDE.md                    注入三拍规则
README-task-loop.md          用法说明
.gitignore                   忽略 .ai/task.json 等
```

**职责边界**：`lib-state.sh` 只管 task.json 读写；`guardian.sh` 只管拦截（不写 task.json）；`task-*.sh` 只管状态机切换 + gate（不管拦截）。三者互不越界。

---

## Task 1: 项目骨架与测试框架

**Files:**
- Create: `.gitignore`
- Create: `tests/test-helpers.sh`
- Create: `tests/run-all.sh`
- Create: `tests/test-smoke.sh`
- Create: `.claude/scripts/`、`.claude/commands/`、`.claude/hooks/`、`.ai/memory/draft/`、`.ai/memory/active/` 目录（用 `.gitkeep`）

- [ ] **Step 1: 检查 jq 依赖**

Run: `jq --version`
Expected: 输出版本号（如 `jq-1.6`）。若报命令不存在，安装：Windows 用 `scoop install jq` 或 `choco install jq`，macOS `brew install jq`。**jq 是本系统唯一外部依赖，必须先就绪。**

- [ ] **Step 2: 建 .gitignore**

```gitignore
# 任务运行时状态，不入库
.ai/task.json
.ai/memory/draft/*
!.ai/memory/draft/.gitkeep
.ai/memory/active/*
!.ai/memory/active/.gitkeep
```

- [ ] **Step 3: 建目录骨架（含 .gitkeep 占位）**

Run:
```bash
mkdir -p .claude/scripts .claude/commands .claude/hooks \
         .ai/memory/draft .ai/memory/active tests/fixtures
touch .claude/scripts/.gitkeep .claude/commands/.gitkeep .claude/hooks/.gitkeep \
      .ai/memory/draft/.gitkeep .ai/memory/active/.gitkeep
```

- [ ] **Step 4: 写测试辅助 tests/test-helpers.sh**

```bash
#!/usr/bin/env bash
# 纯 bash 断言辅助，零依赖。每个测试 source 本文件后调用。
TEST_PASS=0
TEST_FAIL=0

assert_eq() {  # actual expected name
  if [ "$1" = "$2" ]; then
    echo "  PASS: $3"; TEST_PASS=$((TEST_PASS+1))
  else
    echo "  FAIL: $3 -- expected [$2] got [$1]"; TEST_FAIL=$((TEST_FAIL+1))
  fi
}

assert_match() {  # pattern text name   (text 含 pattern 即过)
  if echo "$2" | grep -q "$1"; then
    echo "  PASS: $3"; TEST_PASS=$((TEST_PASS+1))
  else
    echo "  FAIL: $3 -- [$2] 不含 [$1]"; TEST_FAIL=$((TEST_FAIL+1))
  fi
}

assert_exit() {  # expected_code cmd...  (运行 cmd，断言退出码)
  local exp="$1"; shift
  "$@" >/dev/null 2>&1; local code=$?
  if [ "$code" = "$exp" ]; then
    echo "  PASS: 退出码 $exp ($*)"; TEST_PASS=$((TEST_PASS+1))
  else
    echo "  FAIL: 期望退出码 $exp 实际 $code ($*)"; TEST_FAIL=$((TEST_FAIL+1))
  fi
}

summary() {
  echo "---- $TEST_PASS passed, $TEST_FAIL failed ----"
  [ "$TEST_FAIL" = 0 ]
}
```

- [ ] **Step 5: 写 tests/run-all.sh**

```bash
#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/.."
for t in tests/test-*.sh; do
  echo "=== $t ==="
  bash "$t" || { echo "FAILED: $t"; exit 1; }
done
echo "ALL TESTS PASSED"
```

- [ ] **Step 6: 写冒烟测试 tests/test-smoke.sh，确认框架跑通**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
assert_eq "1" "1" "框架基本断言"
assert_eq "$(jq -n '1+1')" "2" "jq 可用"
summary
```

- [ ] **Step 7: 跑冒烟测试**

Run: `bash tests/run-all.sh`
Expected: 打印 `=== tests/test-smoke.sh ===`、两个 PASS、`2 passed, 0 failed`、`ALL TESTS PASSED`。

- [ ] **Step 8: Commit**

```bash
git add .gitignore .claude/ .ai/ tests/
git commit -m "feat: 项目骨架与零依赖测试框架"
```

---

## Task 2: 状态库 lib-state.sh

**Files:**
- Create: `.claude/scripts/lib-state.sh`
- Create: `tests/test-lib-state.sh`
- Create: `tests/fixtures/task-plan.json`

**说明**：`lib-state.sh` 提供读写 task.json 的函数，被 guardian 和 task-*.sh 共同 source。它是唯一触碰 task.json 内容的地方（guardian 只读，task-*.sh 读写）。

- [ ] **Step 1: 写测试 fixtures/tests/fixtures/task-plan.json**

```json
{
  "task_id": "TASK-20260617-test",
  "stage": "PLAN",
  "human_owner": "tester",
  "scope": { "allowed_paths": ["src/pay/refund/"], "blocked_paths": ["src/auth/"], "extra_grants": [] },
  "gate": { "plan_confirmed": false, "pre_committed": false, "tests_passed": false, "review_done": false, "post_committed": false }
}
```

- [ ] **Step 2: 写失败测试 tests/test-lib-state.sh**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
TASK_FILE="tests/fixtures/task-plan.json"
source .claude/scripts/lib-state.sh

assert_eq "$(state_get_stage "$TASK_FILE")" "PLAN" "读 stage"
assert_eq "$(state_get_scope allowed_paths "$TASK_FILE")" "src/pay/refund/" "读 allowed"
assert_eq "$(state_get_scope blocked_paths "$TASK_FILE")" "src/auth/" "读 blocked"

# 临时副本测写入，不污染 fixture
cp "$TASK_FILE" /tmp/t.json
state_set_stage /tmp/t.json BUILD
assert_eq "$(jq -r '.stage' /tmp/t.json)" "BUILD" "写 stage"

state_extend /tmp/t.json "src/util/"
assert_match "src/util/" "$(jq -rc '.scope.extra_grants[]' /tmp/t.json)" "追加 extra"

state_clear_scope /tmp/t.json
assert_eq "$(jq -rc '.scope.allowed_paths' /tmp/t.json)" "[]" "清空 scope"

summary
```

- [ ] **Step 3: 跑测试确认失败**

Run: `bash tests/test-lib-state.sh`
Expected: FAIL（lib-state.sh 还不存在 / 函数未定义）。

- [ ] **Step 4: 实现 .claude/scripts/lib-state.sh**

```bash
#!/usr/bin/env bash
# task.json 读写辅助。所有对 task.json 的访问都经这里，便于统一加日志/校验。
# 注意：guardian 只调用读函数；只有 task-*.sh 调用写函数。

state_get_stage() {  # task_file -> stage
  jq -r '.stage' "$1"
}

state_get_scope() {  # key task_file -> 逐行输出数组元素
  jq -r ".scope.$1[]?" "$2"
}

state_get_gate() {  # key task_file -> true/false
  jq -r ".gate.$1" "$2"
}

state_set_stage() {  # task_file stage  （原地改写）
  local f="$1" s="$2"
  local tmp; tmp="$(mktemp)"
  jq --arg s "$s" '.stage = $s' "$f" > "$tmp" && mv "$tmp" "$f"
}

state_set_gate() {  # task_file key value
  local f="$1" k="$2" v="$3"
  local tmp; tmp="$(mktemp)"
  jq --arg k "$k" --argjson v "$v" '.gate[$k] = $v' "$f" > "$tmp" && mv "$tmp" "$f"
}

state_extend() {  # task_file path  （追加到 extra_grants）
  local f="$1" p="$2"
  local tmp; tmp="$(mktemp)"
  jq --arg p "$p" '.scope.extra_grants += [$p]' "$f" > "$tmp" && mv "$tmp" "$f"
}

state_clear_scope() {  # task_file  （清 allowed/blocked/extra，任务结束用）
  local f="$1"
  local tmp; tmp="$(mktemp)"
  jq '.scope = {allowed_paths:[], blocked_paths:[], extra_grants:[]}' "$f" > "$tmp" && mv "$tmp" "$f"
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `bash tests/test-lib-state.sh`
Expected: 全部 PASS，`0 failed`。

- [ ] **Step 6: Commit**

```bash
git add .claude/scripts/lib-state.sh tests/test-lib-state.sh tests/fixtures/task-plan.json
git commit -m "feat: task.json 状态读写库 lib-state"
```

---

## Task 3: guardian 核心——命门与无任务拦截

**Files:**
- Create: `.claude/hooks/guardian.sh`
- Create: `tests/test-guardian.sh`

**说明**：guardian 的骨架。先实现两条最硬的规则：① `.ai/task.json` 永远不可被 AI 写（命门）；② 无 task.json 时拦所有写（提示先 /lock）。后续 task 再加 PLAN/BUILD/CLOSE 分支。

guardian 从 stdin 读 PreToolUse JSON，函数 `rel_path` 把绝对路径转相对项目根路径。

- [ ] **Step 1: 写失败测试 tests/test-guardian.sh（先只测命门 + 无任务）**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
GUARD=".claude/hooks/guardian.sh"

# 工具：构造 PreToolUse stdin 并跑 guardian，返回退出码
run_guard() {  # tool_name file_path  -> 退出码
  local payload; payload=$(jq -nc --arg t "$1" --arg p "$2" \
    '{tool_name:$t, tool_input:{file_path:$p}}')
  echo "$payload" | bash "$GUARD" >/dev/null 2>&1; echo $?
}

# 1. 无 task.json -> 拦所有写
rm -f .ai/task.json
assert_eq "$(run_guard Write src/any.go)" "2" "无任务时拦写"

# 2. 有 task.json，但写 task.json 自身 -> 命门，拦
cp tests/fixtures/task-plan.json .ai/task.json
assert_eq "$(run_guard Write .ai/task.json)" "2" "命门:写 task.json 被拦"
assert_eq "$(run_guard Write /abs/.ai/task.json)" "2" "命门:绝对路径写 task.json 被拦"

rm -f .ai/task.json
summary
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bash tests/test-guardian.sh`
Expected: FAIL（guardian.sh 不存在）。

- [ ] **Step 3: 实现 .claude/hooks/guardian.sh（骨架版）**

```bash
#!/usr/bin/env bash
set -uo pipefail
# PreToolUse 守卫。读 stdin JSON，按 task.json 的 stage + scope 拦截写类工具。
# 退出码: 0 放行, 2 阻断(stderr 反馈给 AI)。

TASK_FILE=".ai/task.json"

# 无任务 -> 拦所有写
if [ ! -f "$TASK_FILE" ]; then
  echo "GUARDIAN: 无活动任务，先用 /lock 锁定范围后再写文件。" >&2
  exit 2
fi

INPUT="$(cat)"
TOOL_NAME="$(echo "$INPUT" | jq -r '.tool_name // empty')"
# fail-closed: 解析不出工具名，阻断（宁可误拦不可漏放）
[ -n "$TOOL_NAME" ] || { echo "GUARDIAN: 无法解析工具调用(fail-closed)" >&2; exit 2; }

# 只拦截写类工具，其余放行
case "$TOOL_NAME" in
  Write|Edit|NotebookEdit) ;;
  Bash)  exit 0 ;;   # Bash 弱约束在 Task 6 接入
  *)     exit 0 ;;
esac

# 目标路径（Write/Edit 用 file_path，Notebook 用 notebook_path）
TARGET="$(echo "$INPUT" | jq -r '.tool_input.file_path // .tool_input.notebook_path // empty')"
# fail-closed: 写类工具但读不到目标路径，阻断
[ -n "$TARGET" ] || { echo "GUARDIAN: 无法解析写入目标(fail-closed)" >&2; exit 2; }
# 绝对路径 -> 相对项目根
REL="${TARGET#"$PWD"/}"
REL="${REL:-$TARGET}"

# 命门：task.json 对 AI 永远只读
case "$REL" in
  .ai/task.json|*/task.json|.ai/task.json)
    echo "GUARDIAN: .ai/task.json 是系统状态文件，AI 不可修改。" >&2
    exit 2 ;;
esac

# TODO(Task4-6): stage 分支（PLAN/BUILD/CLOSE 白名单 + blocked 硬禁区）
exit 0
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bash tests/test-guardian.sh`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add .claude/hooks/guardian.sh tests/test-guardian.sh
git commit -m "feat: guardian 骨架——命门拦截与无任务保护"
```

---

## Task 4: guardian——blocked 硬禁区 + PLAN 白名单

**Files:**
- Modify: `.claude/hooks/guardian.sh`
- Modify: `tests/test-guardian.sh`

**说明**：接入 blocked 前缀匹配（铁律，全阶段生效）+ PLAN 阶段白名单（只能写 `.ai/plan/` 和 `.ai/memory/draft/`）。

- [ ] **Step 1: 追加测试（在 test-guardian.sh 的 `rm -f .ai/task.json` 之前插入）**

```bash
# 3. blocked 硬禁区：写 src/auth/ 被拦（fixture 里 blocked_paths=["src/auth/"]）
cp tests/fixtures/task-plan.json .ai/task.json
assert_eq "$(run_guard Write src/auth/login.go)" "2" "blocked 路径被拦"

# 4. PLAN 白名单：写方案放行，写业务代码被拦
assert_eq "$(run_guard Write .ai/plan/notes.md)" "0" "PLAN 写方案放行"
assert_eq "$(run_guard Write .ai/memory/draft/MEM-x.md)" "0" "PLAN 写知识候选放行"
assert_eq "$(run_guard Write src/pay/refund/handler.go)" "2" "PLAN 写业务代码被拦"
```

- [ ] **Step 2: 跑测试确认新增项失败**

Run: `bash tests/test-guardian.sh`
Expected: 新增 4 项 FAIL（PLAN 分支还没实现，代码路径都 exit 0 放行了）。

- [ ] **Step 3: 实现——在 guardian.sh 的命门 case 之后、TODO 处替换为完整逻辑**

```bash
source .claude/scripts/lib-state.sh

STAGE="$(state_get_stage "$TASK_FILE")"
ALLOWED="$(state_get_scope allowed_paths "$TASK_FILE")"
BLOCKED="$(state_get_scope blocked_paths "$TASK_FILE")"
EXTRA="$(state_get_scope extra_grants "$TASK_FILE")"

# 前缀匹配: 路径以任一前缀开头则命中
matches_prefix() {  # path  前缀们来自 stdin
  local p="$1" pat
  while IFS= read -r pat; do
    [ -n "$pat" ] && [[ "$p" == "$pat"* ]] && return 0
  done
  return 1
}

# blocked 铁律: 全阶段生效
if echo "$BLOCKED" | matches_prefix "$REL"; then
  echo "GUARDIAN: $REL 在硬禁区(blocked)，不可修改，/extend 也放不了。" >&2
  exit 2
fi

case "$STAGE" in
  PLAN)
    # 白名单: 方案 + 知识候选
    printf '%s\n.ai/plan/' '.ai/memory/draft/' | matches_prefix "$REL" \
      && exit 0
    echo "GUARDIAN: PLAN 阶段只能写方案(.ai/plan/)和知识候选(.ai/memory/draft/)。确认计划后用 /build 进入 BUILD。" >&2
    exit 2 ;;
  BUILD)
    # 白名单: allowed + extra
    { echo "$ALLOWED"; echo "$EXTRA"; } | matches_prefix "$REL" \
      && exit 0
    echo "GUARDIAN: $REL 超出锁定范围。需要时用 /extend <path> 申请放行。" >&2
    exit 2 ;;
  CLOSE)
    # 白名单: 知识候选 + 文档(*.md)
    { printf '%s\n.ai/memory/draft/'; } | matches_prefix "$REL" && exit 0
    case "$REL" in *.md) exit 0 ;; esac
    echo "GUARDIAN: CLOSE 阶段已冻结实现代码，只能写知识候选和文档。要改代码先 /build 退回。" >&2
    exit 2 ;;
  DONE|*)
    echo "GUARDIAN: 任务已 DONE，无写入权限。新任务用 /lock。" >&2
    exit 2 ;;
esac
```

注意：`source lib-state.sh` 要移到文件顶部（命门 case 之前），因为现在要用 `state_get_*`。

- [ ] **Step 4: 跑全部 guardian 测试确认通过**

Run: `bash tests/test-guardian.sh`
Expected: 全部 PASS（含 Task3 的命门项 + 本任务新增项）。

- [ ] **Step 5: Commit**

```bash
git add .claude/hooks/guardian.sh tests/test-guardian.sh
git commit -m "feat: guardian 接入 blocked 硬禁区与三拍白名单"
```

---

## Task 5: guardian——补全测试矩阵 + 路径前缀规范化

**Files:**
- Modify: `tests/test-guardian.sh`
- Create: `tests/fixtures/task-build.json`、`tests/fixtures/task-close.json`

**说明**：补齐 BUILD / CLOSE 阶段的代表性 case，并验证绝对路径、`extra_grants` 放行、CLOSE 写代码被拦。这一步不加新代码，只是把 guardian 的行为锁死在测试里（回归保护）。

- [ ] **Step 1: 建 BUILD/CLOSE fixtures**

`tests/fixtures/task-build.json`：复制 task-plan.json，把 `"stage"` 改 `"BUILD"`，`allowed_paths` 保持 `["src/pay/refund/"]`，`extra_grants` 设 `["src/util/helper.go"]`。

`tests/fixtures/task-close.json`：stage 改 `"CLOSE"`。

- [ ] **Step 2: 追加测试（test-guardian.sh 末尾 summary 前）**

```bash
# 5. BUILD: allowed 放行、extra 放行、范围外拦、绝对路径匹配
cp tests/fixtures/task-build.json .ai/task.json
assert_eq "$(run_guard Write src/pay/refund/x.go)" "0" "BUILD 写 allowed 放行"
assert_eq "$(run_guard Write src/util/helper.go)" "0" "BUILD 写 extra 放行"
assert_eq "$(run_guard Write src/other/y.go)" "2" "BUILD 写范围外拦"
assert_eq "$(run_guard Write "$PWD/src/pay/refund/x.go")" "0" "BUILD 绝对路径在 allowed 放行"

# 6. CLOSE: 文档放行、知识候选放行、代码拦
cp tests/fixtures/task-close.json .ai/task.json
assert_eq "$(run_guard Write docs/api.md)" "0" "CLOSE 写 md 放行"
assert_eq "$(run_guard Write .ai/memory/draft/MEM-z.md)" "0" "CLOSE 写知识候选放行"
assert_eq "$(run_guard Write src/pay/refund/x.go)" "2" "CLOSE 写代码拦"

rm -f .ai/task.json
```

- [ ] **Step 3: 跑测试**

Run: `bash tests/test-guardian.sh`
Expected: 全部 PASS。若有 FAIL，是 guardian 路径规范化或前缀匹配的 bug，修到通过。

- [ ] **Step 4: Commit**

```bash
git add tests/
git commit -m "test: 补全 guardian 三拍路径匹配回归矩阵"
```

---

## Task 6: guardian——Bash 写操作弱约束

**Files:**
- Modify: `.claude/hooks/guardian.sh`
- Modify: `tests/test-guardian.sh`

**说明**：Bash 命令是否写文件不可靠判定（spec 风险①）。采用弱约束：检测高危写命令词（`rm`、`mv`、`cp`、`>`、`sed -i`、`tee`）命中且当前在 PLAN/CLOSE 阶段 → 提示并拦截；BUILD 阶段对 Bash 放行（BUILD 本就允许写）。这是最小版的诚实兜底。

- [ ] **Step 1: 追加测试**

```bash
# 工具: 跑 Bash 工具的 guardian
run_guard_bash() {  # stage_fixture  command  -> 退出码
  cp "tests/fixtures/$1" .ai/task.json
  local payload; payload=$(jq -nc --arg c "$2" '{tool_name:"Bash",tool_input:{command:$c}}')
  echo "$payload" | bash "$GUARD" >/dev/null 2>&1; echo $?
}

# 7. PLAN 阶段高危 bash 被拦
assert_eq "$(run_guard_bash task-plan.json 'rm src/x.go')" "2" "PLAN: rm 被拦"
assert_eq "$(run_guard_bash task-plan.json 'sed -i s/a/b/ src/x.go')" "2" "PLAN: sed -i 被拦"
assert_eq "$(run_guard_bash task-plan.json 'ls -la')" "0" "PLAN: 只读命令放行"
# 8. BUILD 阶段 bash 放行（BUILD 允许写）
assert_eq "$(run_guard_bash task-build.json 'rm src/pay/refund/x.go')" "0" "BUILD: bash 放行"
rm -f .ai/task.json
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bash tests/test-guardian.sh`
Expected: 新增项里 PLAN 的 rm/sed 应 FAIL（当前 Bash 直接 exit 0）。

- [ ] **Step 3: 实现——把 guardian.sh 里的 `Bash) exit 0 ;;` 替换为**

```bash
  Bash)
    CMD="$(echo "$INPUT" | jq -r '.tool_input.command // empty')"
    STAGE_B="$(state_get_stage "$TASK_FILE")"
    # BUILD/DONE 放行 bash（BUILD 允许写；DONE 无所谓，写也会被文件层拦）
    case "$STAGE_B" in BUILD|DONE) exit 0 ;; esac
    # PLAN/CLOSE: 检测高危写命令
    if echo "$CMD" | grep -qE '(^|[[:space:]])(rm|mv|cp|tee)([[:space:]]|$)|>|>>|sed[[:space:]].*-i'; then
      echo "GUARDIAN: 当前 $STAGE_B 阶段不允许通过 Bash 改文件。需要写代码请先 /build。" >&2
      exit 2
    fi
    exit 0 ;;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bash tests/test-guardian.sh`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add .claude/hooks/guardian.sh tests/test-guardian.sh
git commit -m "feat: guardian 对 Bash 高危写命令弱约束(PLAN/CLOSE)"
```

---

## Task 7: task-lock.sh（/lock 初始化 + 进 PLAN）

**Files:**
- Create: `.claude/scripts/task-lock.sh`
- Create: `tests/test-lock.sh`

**说明**：`/lock <paths...>` 用传入的路径前缀初始化 task.json，stage=PLAN。人发车的起点。

- [ ] **Step 1: 写失败测试 tests/test-lock.sh**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
rm -f .ai/task.json

# lock 两个路径
bash .claude/scripts/task-lock.sh src/pay/refund/ src/util/
assert_eq "$?" "0" "lock 成功退出"
assert_eq "$(jq -r '.stage' .ai/task.json)" "PLAN" "lock 后 stage=PLAN"
assert_match "src/pay/refund/" "$(jq -rc '.scope.allowed_paths' .ai/task.json)" "allowed 含 refund"
assert_match "src/util/" "$(jq -rc '.scope.allowed_paths' .ai/task.json)" "allowed 含 util"
assert_match "TASK-" "$(jq -r '.task_id' .ai/task.json)" "生成 task_id"

rm -f .ai/task.json
summary
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bash tests/test-lock.sh`
Expected: FAIL（脚本不存在）。

- [ ] **Step 3: 实现 .claude/scripts/task-lock.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail
# /lock <paths...>  初始化 task.json，进入 PLAN。
cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

if [ "$#" -lt 1 ]; then
  echo "用法: /lock <path1> [path2 ...]   例如 /lock src/pay/refund/" >&2
  exit 1
fi

mkdir -p .ai/memory/draft .ai/memory/active

# 构造 allowed_paths 的 JSON 数组
ALLOWED=$(printf '%s\n' "$@" | jq -R . | jq -s .)

DATE=$(date +%Y%m%d)
SLUG=$(echo "$1" | tr -d '/' | cut -c1-20)
TID="TASK-${DATE}-${SLUG}"

jq -n \
  --arg id "$TID" \
  --argjson allowed "$ALLOWED" \
  '{
    task_id: $id, stage: "PLAN", human_owner: "",
    scope: { allowed_paths: $allowed, blocked_paths: [], extra_grants: [] },
    gate: { plan_confirmed: false, pre_committed: false, tests_passed: false, review_done: false, post_committed: false }
  }' > .ai/task.json

echo "已锁定范围: $*"
echo "stage=PLAN。写方案到 .ai/plan/，前置知识到 .ai/memory/draft/。"
echo "确认计划后用 /build 进入 BUILD。"
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bash tests/test-lock.sh`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add .claude/scripts/task-lock.sh tests/test-lock.sh
git commit -m "feat: task-lock 初始化任务并进入 PLAN"
```

---

## Task 8: task-build.sh（gate + 进 BUILD）

**Files:**
- Create: `.claude/scripts/task-build.sh`
- Create: `tests/test-build.sh`

**说明**：`/build` 检查 gate（`plan_confirmed` + `pre_committed`），通过则 stage=BUILD。`pre_committed` 由"检测 memory/draft/ 有文件"自动判定；`plan_confirmed` 由人通过参数确认（`/build --confirm`）。

- [ ] **Step 1: 写失败测试 tests/test-build.sh**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
rm -rf .ai/memory/draft/*; mkdir -p .ai/memory/draft

# case1: 无前置知识 + 未确认 -> 拒
cp tests/fixtures/task-plan.json .ai/task.json
bash .claude/scripts/task-build.sh --confirm 2>/dev/null; assert_eq "$?" "1" "无前置知识时拒"
assert_eq "$(jq -r '.stage' .ai/task.json)" "PLAN" "未过 gate 不切 stage"

# case2: 有前置知识 + 确认 -> 进 BUILD
echo "# 前置知识" > .ai/memory/draft/MEM-pre.md
bash .claude/scripts/task-build.sh --confirm 2>/dev/null; assert_eq "$?" "0" "过 gate 成功"
assert_eq "$(jq -r '.stage' .ai/task.json)" "BUILD" "切到 BUILD"
assert_eq "$(jq -r '.gate.pre_committed' .ai/task.json)" "true" "pre_committed 置 true"

# case3: 有知识但没 --confirm -> 拒（人没确认计划）
cp tests/fixtures/task-plan.json .ai/task.json
bash .claude/scripts/task-build.sh 2>/dev/null; assert_eq "$?" "1" "缺 --confirm 被拒"

rm -f .ai/task.json; rm -f .ai/memory/draft/*
summary
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bash tests/test-build.sh`
Expected: FAIL（脚本不存在）。

- [ ] **Step 3: 实现 .claude/scripts/task-build.sh**

```bash
#!/usr/bin/env bash
set -uo pipefail
cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
source .claude/scripts/lib-state.sh
TF=".ai/task.json"

[ -f "$TF" ] || { echo "无活动任务，先 /lock。" >&2; exit 1; }
[ "$(state_get_stage "$TF")" = "PLAN" ] || { echo "当前非 PLAN 阶段，无需 /build。" >&2; exit 1; }

CONFIRM=0
[ "${1:-}" = "--confirm" ] && CONFIRM=1

# pre_committed: 检测 draft/ 有文件（.gitkeep 不算）
PRE=0
if ls .ai/memory/draft/* 2>/dev/null | grep -qv '\.gitkeep'; then PRE=1; fi

MISSING=""
[ "$CONFIRM" = 1 ] || MISSING="plan_confirmed(加 --confirm 确认计划)"
[ "$PRE" = 1 ] || MISSING="$MISSING pre_committed(先在 .ai/memory/draft/ 写前置知识)"

if [ -n "$MISSING" ]; then
  echo "/build 未通过 gate，缺:$MISSING" >&2
  exit 1
fi

state_set_gate "$TF" plan_confirmed true
state_set_gate "$TF" pre_committed true
state_set_stage "$TF" BUILD
echo "stage=BUILD。只能在 allowed/extra 范围内写代码。开发完成用 /close。"
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bash tests/test-build.sh`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add .claude/scripts/task-build.sh tests/test-build.sh
git commit -m "feat: task-build gate 校验并进入 BUILD"
```

---

## Task 9: task-close.sh（gate + 进 CLOSE/DONE）

**Files:**
- Create: `.claude/scripts/task-close.sh`
- Create: `tests/test-close.sh`

**说明**：`/close` 行为分两种——
- 当前 BUILD：检查 `tests_passed` + `review_done`，通过则进 CLOSE。
- 当前 CLOSE：检查 `post_committed`（draft/ 有新文件），通过则进 DONE 并清 scope。

`tests_passed` 由 `--tested`、`review_done` 由 `--reviewed` 显式带（无测试项目用 `--tested` 人为确认）。

- [ ] **Step 1: 写失败测试 tests/test-close.sh**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
rm -rf .ai/memory/draft/*; mkdir -p .ai/memory/draft

# case1: BUILD 缺 gate -> 拒
cp tests/fixtures/task-build.json .ai/task.json
bash .claude/scripts/task-close.sh 2>/dev/null; assert_eq "$?" "1" "BUILD 缺 gate 拒"
# case2: BUILD 带 --tested --reviewed -> 进 CLOSE
bash .claude/scripts/task-close.sh --tested --reviewed 2>/dev/null; assert_eq "$?" "0" "过 gate"
assert_eq "$(jq -r '.stage' .ai/task.json)" "CLOSE" "进 CLOSE"

# case3: CLOSE 无后置知识 -> 拒
bash .claude/scripts/task-close.sh 2>/dev/null; assert_eq "$?" "1" "CLOSE 缺后置知识拒"
# case4: CLOSE 有后置知识 -> DONE + 清 scope
echo "# 后置知识" > .ai/memory/draft/MEM-post.md
bash .claude/scripts/task-close.sh 2>/dev/null; assert_eq "$?" "0" "CLOSE 完成"
assert_eq "$(jq -r '.stage' .ai/task.json)" "DONE" "进 DONE"
assert_eq "$(jq -rc '.scope.allowed_paths' .ai/task.json)" "[]" "scope 已清"

rm -f .ai/task.json; rm -f .ai/memory/draft/*
summary
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bash tests/test-close.sh`
Expected: FAIL（脚本不存在）。

- [ ] **Step 3: 实现 .claude/scripts/task-close.sh**

```bash
#!/usr/bin/env bash
set -uo pipefail
cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
source .claude/scripts/lib-state.sh
TF=".ai/task.json"

[ -f "$TF" ] || { echo "无活动任务，先 /lock。" >&2; exit 1; }
STAGE="$(state_get_stage "$TF")"

draft_has_files() { ls .ai/memory/draft/* 2>/dev/null | grep -qv '\.gitkeep'; }

case "$STAGE" in
  BUILD)
    TESTED=0; REVIEWED=0
    for a in "$@"; do
      case "$a" in --tested) TESTED=1;; --reviewed) REVIEWED=1;; esac
    done
    MISSING=""
    [ "$TESTED" = 1 ] || MISSING=" tests_passed(加 --tested)"
    [ "$REVIEWED" = 1 ] || MISSING="$MISSING review_done(加 --reviewed)"
    if [ -n "$MISSING" ]; then echo "/close 未过 gate:$MISSING" >&2; exit 1; fi
    state_set_gate "$TF" tests_passed true
    state_set_gate "$TF" review_done true
    state_set_stage "$TF" CLOSE
    echo "stage=CLOSE。把后置知识写到 .ai/memory/draft/，再 /close 完成。" ;;
  CLOSE)
    if ! draft_has_files; then
      echo "/close 未过 gate: 缺后置知识(先在 .ai/memory/draft/ 写)" >&2; exit 1
    fi
    state_set_gate "$TF" post_committed true
    state_set_stage "$TF" DONE
    state_clear_scope "$TF"
    echo "stage=DONE，scope 已清空。draft/ 中的知识待人类审查后激活到 memory/active/。" ;;
  *)
    echo "当前 $STAGE，/close 不适用。" >&2; exit 1 ;;
esac
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bash tests/test-close.sh`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add .claude/scripts/task-close.sh tests/test-close.sh
git commit -m "feat: task-close 双段 gate(BUILD->CLOSE->DONE)"
```

---

## Task 10: task-extend.sh（放行口子）

**Files:**
- Create: `.claude/scripts/task-extend.sh`
- Create: `tests/test-extend.sh`

**说明**：`/extend <path>` 把路径追加进 `extra_grants`，guardian 下次放行。blocked 路径拒绝追加（铁律）。

- [ ] **Step 1: 写失败测试 tests/test-extend.sh**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh

# case1: extend 普通路径 -> extra 增
cp tests/fixtures/task-build.json .ai/task.json
bash .claude/scripts/task-extend.sh src/util/ 2>/dev/null; assert_eq "$?" "0" "extend 成功"
assert_match "src/util/" "$(jq -rc '.scope.extra_grants[]' .ai/task.json)" "extra 含新路径"
# guardian 现在放行该路径
echo '{"tool_name":"Write","tool_input":{"file_path":"src/util/x.go"}}' \
  | bash .claude/hooks/guardian.sh >/dev/null 2>&1; assert_eq "$?" "0" "extend 后 guardian 放行"

# case2: extend blocked 路径 -> 拒
cp tests/fixtures/task-build.json .ai/task.json
jq '.scope.blocked_paths=["src/auth/"]' .ai/task.json > t && mv t .ai/task.json
bash .claude/scripts/task-extend.sh src/auth/ 2>/dev/null; assert_eq "$?" "1" "extend blocked 被拒"
assert_eq "$(jq -rc '.scope.extra_grants' .ai/task.json)" "[]" "blocked 未进 extra"

rm -f .ai/task.json
summary
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bash tests/test-extend.sh`
Expected: FAIL（脚本不存在）。

- [ ] **Step 3: 实现 .claude/scripts/task-extend.sh**

```bash
#!/usr/bin/env bash
set -uo pipefail
cd "$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
source .claude/scripts/lib-state.sh
TF=".ai/task.json"

[ -f "$TF" ] || { echo "无活动任务。" >&2; exit 1; }
[ -n "${1:-}" ] || { echo "用法: /extend <path>" >&2; exit 1; }
P="$1"

# 铁律: blocked 不可放行
if state_get_scope blocked_paths "$TF" | grep -qxF "$P" 2>/dev/null \
   || echo "$(state_get_scope blocked_paths "$TF")" | grep -q "^${P%%/*}"; then
  : # 粗略匹配，下面用前缀更稳
fi
BLOCKED="$(state_get_scope blocked_paths "$TF")"
while IFS= read -r b; do
  [ -n "$b" ] && [[ "$P" == "$b"* ]] && { echo "/extend 拒绝: $P 在 blocked 硬禁区。" >&2; exit 1; }
done <<< "$BLOCKED"

state_extend "$TF" "$P"
echo "已放行: $P (临时，任务结束清空)"
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bash tests/test-extend.sh`
Expected: 全部 PASS。（若 blocked 匹配有误报/漏报，调整前缀逻辑到通过。）

- [ ] **Step 5: Commit**

```bash
git add .claude/scripts/task-extend.sh tests/test-extend.sh
git commit -m "feat: task-extend 放行口子(blocked 不可放行)"
```

---

## Task 11: slash command 触发器（4 个）

**Files:**
- Create: `.claude/commands/lock.md`、`build.md`、`close.md`、`extend.md`

**说明**：Claude Code 的 slash command 是 markdown，内容是给 AI 的 prompt。这里让命令触发对应脚本并把输出反馈。

- [ ] **Step 1: 写 .claude/commands/lock.md**

```markdown
---
description: 锁定本次改动范围并进入 PLAN 阶段
---

执行任务闭环的锁定步骤。运行以下命令并报告结果：

`bash .claude/scripts/task-lock.sh $ARGUMENTS`

然后告知用户：当前进入 PLAN 阶段，只能写 `.ai/plan/`（方案）和 `.ai/memory/draft/`（前置知识），不能动业务代码。写完方案和前置知识后用 `/build` 进入开发。
```

- [ ] **Step 2: 写 .claude/commands/build.md**

```markdown
---
description: 校验 gate 并从 PLAN 进入 BUILD
---

运行：`bash .claude/scripts/task-build.sh $ARGUMENTS`

若脚本退出码非 0，把 stderr 原样转告用户并停下，不要继续。若成功，告知用户已进入 BUILD，只能在锁定范围内写代码，越界需 `/extend` 申请。开发完成用 `/close`。
```

- [ ] **Step 3: 写 .claude/commands/close.md**

```markdown
---
description: 校验 gate 并推进到 CLOSE / DONE
---

运行：`bash .claude/scripts/task-close.sh $ARGUMENTS`

根据脚本输出判断：若提示进入 CLOSE，提醒用户把后置知识写到 `.ai/memory/draft/` 后再敲一次 `/close` 收尾；若提示进入 DONE，提醒用户审查 draft/ 并把值得保留的知识激活到 `memory/active/`。
```

- [ ] **Step 4: 写 .claude/commands/extend.md**

```markdown
---
description: 临时放行一个锁定范围外的路径
---

运行：`bash .claude/scripts/task-extend.sh $ARGUMENTS`

若脚本拒绝（路径在 blocked 硬禁区），转告用户该路径不可放行。
```

- [ ] **Step 5: 人工验证命令格式（无法自动化测 slash command 解析，确认 frontmatter 合法即可）**

Run: `for f in .claude/commands/*.md; do head -1 "$f"; done`
Expected: 4 个文件第一行都是 `---`。

- [ ] **Step 6: Commit**

```bash
git add .claude/commands/
git commit -m "feat: 四个 slash command 触发器"
```

---

## Task 12: settings.json 注册 guardian + CLAUDE.md 注入规则

**Files:**
- Create: `.claude/settings.json`
- Create: `CLAUDE.md`（项目根，若已有则在顶部追加任务闭环章节）

**说明**：把 guardian 挂到 PreToolUse；CLAUDE.md 让 AI 知道三拍规则、当前任务状态在 `.ai/task.json`、被 guardian 拦了该怎么办。

- [ ] **Step 1: 写 .claude/settings.json**

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit|Bash",
        "hooks": [
          { "type": "command", "command": "bash .claude/hooks/guardian.sh" }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 写 CLAUDE.md（任务闭环章节）**

```markdown
# 任务闭环系统 (Task Loop)

本仓库启用单任务闭环。AI 的所有文件写入受 `.claude/hooks/guardian.sh` 守卫。

## 当前任务
状态文件：`.ai/task.json`（AI 只读，不可修改——改了会被 guardian 拦）。
若 `.ai/task.json` 不存在，说明没有活动任务，先让用户 `/lock <paths>`。

## 三拍
- **PLAN**：只能写 `.ai/plan/`（方案）和 `.ai/memory/draft/`（前置知识）。不动业务代码。
- **BUILD**：只能在 `allowed_paths`+`extra_grants` 范围内写代码。越界被拦时，告诉用户需要 `/extend <path>`。
- **CLOSE**：实现代码冻结，只能写知识候选和文档。

## 切换靠人发车
`/lock` → `/build --confirm` → `/close --tested --reviewed` → `/close`（收尾）。AI 不自行推进阶段。

## 知识
两次提交都写到 `.ai/memory/draft/`：PLAN 末写前置知识（为什么这么改），CLOSE 写后置知识（踩的坑、新决策）。格式沿用 `docs/.../03-memory-ledger.md` 的 MEM-* 模板。AI 只写 draft，激活到 `active/` 由人决定。

## 被 guardian 拦了怎么办
读 stderr 的 GUARDIAN 提示，按提示行动（申请 /extend、或退回上阶段），不要反复重试相同动作。
```

> 若项目根已有 CLAUDE.md，把以上内容作为新章节追加，不要覆盖已有内容。

- [ ] **Step 3: 验证 settings.json 是合法 JSON**

Run: `jq empty .claude/settings.json && echo OK`
Expected: `OK`（无解析错误）。

- [ ] **Step 4: Commit**

```bash
git add .claude/settings.json CLAUDE.md
git commit -m "feat: 注册 guardian hook 与 CLAUDE.md 三拍规则"
```

---

## Task 13: 端到端测试 + 用法文档

**Files:**
- Create: `tests/test-e2e.sh`
- Create: `README-task-loop.md`

**说明**：把 lock → 写方案 → build → 写代码（含越界/extend）→ close → 收尾 串成一次完整任务，验证整条链路。

- [ ] **Step 1: 写端到端测试 tests/test-e2e.sh**

```bash
#!/usr/bin/env bash
cd "$(dirname "$0")/.."
source tests/test-helpers.sh
rm -rf .ai/task.json .ai/plan .ai/memory/draft/*; mkdir -p .ai/plan .ai/memory/draft

run_guard() { echo "$1" | bash .claude/hooks/guardian.sh >/dev/null 2>&1; echo $?; }
wf() { echo "{\"tool_name\":\"Write\",\"tool_input\":{\"file_path\":\"$1\"}}" ; }  # 写文件 payload

# 1. lock
bash .claude/scripts/task-lock.sh src/app/ >/dev/null
assert_eq "$(jq -r '.stage' .ai/task.json)" "PLAN" "e2e: PLAN"
# 2. PLAN 写代码被拦
assert_eq "$(run_guard "$(wf src/app/x.go)")" "2" "e2e: PLAN 写代码拦"
# 3. 写前置知识 -> build -> BUILD
echo pre > .ai/memory/draft/MEM.md
bash .claude/scripts/task-build.sh --confirm >/dev/null
assert_eq "$(jq -r '.stage' .ai/task.json)" "BUILD" "e2e: BUILD"
# 4. 范围外拦 -> extend -> 放行
assert_eq "$(run_guard "$(wf src/other/y.go)")" "2" "e2e: 越界拦"
bash .claude/scripts/task-extend.sh src/other/ >/dev/null
assert_eq "$(run_guard "$(wf src/other/y.go)")" "0" "e2e: extend 后放行"
# 5. close -> CLOSE -> 写后置 -> close -> DONE
bash .claude/scripts/task-close.sh --tested --reviewed >/dev/null
assert_eq "$(jq -r '.stage' .ai/task.json)" "CLOSE" "e2e: CLOSE"
echo post > .ai/memory/draft/MEM-post.md
bash .claude/scripts/task-close.sh >/dev/null
assert_eq "$(jq -r '.stage' .ai/task.json)" "DONE" "e2e: DONE"
assert_eq "$(jq -rc '.scope.allowed_paths' .ai/task.json)" "[]" "e2e: scope 清空"

rm -f .ai/task.json; rm -rf .ai/plan; rm -f .ai/memory/draft/*
summary
```

- [ ] **Step 2: 跑端到端 + 全量测试**

Run: `bash tests/run-all.sh`
Expected: 所有测试文件 PASS，最后 `ALL TESTS PASSED`。

- [ ] **Step 3: 写 README-task-loop.md**

```markdown
# Task Loop —— 单任务闭环系统

一次 AI 开发任务的三拍节奏：**PLAN → BUILD → CLOSE**，人发车，hook 守门。

## 快速开始
1. `/lock src/your/module/`           锁定范围，进入 PLAN
2. 写方案到 `.ai/plan/`，前置知识到 `.ai/memory/draft/`
3. `/build --confirm`                 过 gate，进入 BUILD
4. 在范围内写代码；越界时 `/extend <path>` 申请放行
5. `/close --tested --reviewed`       进入 CLOSE
6. 后置知识写到 `.ai/memory/draft/`，再 `/close` 收尾
7. 审查 `draft/`，值得保留的激活到 `memory/active/`

## 它防住什么
- 范围越界：guardian 硬拦 allowed 外的写入
- 自我放行：`.ai/task.json` 对 AI 只读，AI 改不了自己的状态
- 跳过计划：BUILD 必须 PLAN 末有前置知识 + 人确认
- 经验蒸发：收尾强制写后置知识才能 DONE

## 命中硬禁区(blocked)怎么办
不能放行。这是铁律。如 `src/auth/`、`infra/prod/` 这类，`/extend` 也拒绝。

## 设计文档
`docs/superpowers/specs/2026-06-17-task-loop-design.md`
```

- [ ] **Step 4: Commit**

```bash
git add tests/test-e2e.sh README-task-loop.md
git commit -m "test+docs: 端到端流程测试与用法说明"
```

---

## 完成标准

- [ ] `bash tests/run-all.sh` 全绿（lib-state / guardian / lock / build / close / extend / e2e / smoke）
- [ ] 实际在 Claude Code 里走一次 `/lock → /build → /close` 手动验证 guardian 真的拦得住越界写
- [ ] spec 的每条要求都有对应实现（见下方自审对照）
